# BRAHM Test Log
Generated by: Codex Testing Agent
Started: 2026-04-30 19:30

## Testing Philosophy
Prioritize contract tests around the generator engine, dynamic backend routes, security boundaries, and resilience behavior called out in BRAHM_LOG.md. The system is config-driven, so tests should prove that incomplete or inconsistent JSON is handled as structured output rather than uncaught exceptions. Where implementation behavior differs from the requested spec, document the actual behavior and classify it as either an intentional log-backed change or a failure for human review. No product bugs will be silently fixed during this testing pass.

Phase 0 note: `BRAHM_EXECUTION_BLUEPRINT_v8.md` and the v9.0 patch document were requested but are not present in the workspace. The only root Markdown file discoverable outside `node_modules` is `BRAHM_LOG.md`, which was read fully and used as the authoritative local source for hotfix history.

## Environment
OS: Windows PowerShell workspace at `C:\Users\SANTOSH PANDIT JI\Desktop\Brahm`.
Node: v22.19.0.
npm: 10.9.3.
PostgreSQL client: psql 18.3.
Database reachability: `psql postgresql://brahm:brahm_secret@localhost:5432/brahm_db -c "SELECT 1"` passed.
Docker: unavailable (`docker` command not found), and no compose file is present in the workspace.
Test runner: Jest 30.3.0 installed in `engine`.

Phase 1 notes:
- `npm install` at the workspace root passed.
- `npm ls @brahm/engine --workspaces` passed and resolves the workspace package from backend and frontend.
- `node migrations/migrate.js` exits 0 twice but does not invoke `runMigrations()` directly or print run/skip detail. Existing database state shows five rows in `_migrations`, not the four expected by the original Phase 1 checklist, because migration `005_create_github_credentials.sql` was added by the GitHub PAT patch.
- Backend/frontend resident startup via `Start-Process` failed due a Windows `Path`/`PATH` environment collision. PowerShell jobs did not persist after the command host exited.

## Test Suites
### Engine Unit Tests
Files created:
- `engine/__tests__/normalizer.test.js`
- `engine/__tests__/validator.test.js`
- `engine/__tests__/templates.test.js`
- `engine/__tests__/resilience.test.js`

Command: `npx jest --runInBand` from `engine`.

Result: 72 passed, 3 failed, 75 total.

Coverage:
- E1 shorthand expansion: covered.
- E2 default injection: covered.
- E3 starter component schemas: covered.
- E4 Monaco markers: covered.
- E5 malformed input resilience: covered with failures.
- Template generation: covered with one failure around project ID interpolation.

### Backend Auth and Security API Tests
Files created:
- `platform/backend/__tests__/auth.test.js`

Setup:
- Installed `supertest` and `jest` in `platform/backend`.
- Created `brahm_test`.
- Applied all five migrations to `brahm_test`.
- Test harness launches `node server.js` and targets `http://127.0.0.1:3001`.

Command: `npx jest --runInBand __tests__\auth.test.js` from `platform/backend`.

Sandbox result: failed at setup with `spawn EPERM`.
Escalated result: 11 passed, 3 failed, 14 total.

Coverage:
- B1 registration: partially covered.
- B2 login token/cookie shape: covered and passed.
- B3 refresh rotation: partially covered.
- B4 logout: partially covered.
- B11 security headers: covered and passed.

Important environment finding: although the harness set `DATABASE_URL=postgresql://brahm:brahm_secret@localhost:5432/brahm_test`, the HTTP server wrote users and refresh tokens to `brahm_db`. Direct post-run queries showed `brahm_test.users` was empty and `brahm_db.users` contained `test@example.com`. This makes DB-side assertions unreliable until server test configuration is isolated.

### Frontend Build and Landing E2E Setup
Files created:
- `platform/frontend/playwright.config.js`
- `platform/frontend/e2e/landing.spec.js`

Setup:
- Installed `@playwright/test` in `platform/frontend`.
- Ran `npm run build`.

Build result:
- Sandbox run failed with Vite `spawn EPERM`.
- Escalated run passed. Vite built 81 modules successfully.

Playwright result:
- Sandbox run failed with `spawn EPERM`.
- Escalated configured run discovered 1 test but failed before execution because Chromium was not installed.
- `npx playwright install chromium` failed with `ENOSPC: no space left on device` while downloading the 179.4 MiB browser archive.

Coverage:
- F1 landing page test is authored but not executed successfully because browser installation is blocked by disk capacity.

## Known Failures
### Engine: `normalize(null)` throws
Expected: malformed input resilience should return structured output and never throw.
Actual: `TypeError: Cannot convert undefined or null to object` at `engine/normalizer.js`, when `Object.keys(config)` receives `null`.
Classification: implementation bug against Phase 2 Suite E5 / Phase 5 resilience requirement.

### Engine: `normalize(undefined)` throws
Expected: malformed input resilience should return structured output and never throw.
Actual: `SyntaxError: "undefined" is not valid JSON` at `engine/normalizer.js`, during `JSON.parse(JSON.stringify(parsed))`.
Classification: implementation bug against Phase 2 Suite E5 / Phase 5 resilience requirement.

### Engine Templates: generated frontend code does not embed supplied project ID
Expected: generated frontend code should include the actual project ID passed to `generateTemplates(config, 'project-123')`, or a documented placeholder.
Actual: generated `TaskView.jsx` contains runtime template literals referencing `${projectId}` and `${nameLower}` as literal generated code variables that are not defined in the component output; it does not contain `project-123`.
Classification: implementation bug in template generation, likely to break exported/generated frontend CRUD code.

### Backend Test Isolation: server writes to development database during test run
Expected: backend API tests should use `brahm_test` via `DATABASE_URL`.
Actual: the launched server accepted requests, but DB rows appeared in `brahm_db` while `brahm_test` remained empty. This caused plaintext-password, refresh-expiry, and logout-revocation DB assertions to fail because the test pool queried `brahm_test`.
Classification: testability/configuration risk. The product may be loading `.env` in a way that defeats process-level test configuration in this harness, or the harness environment is being stripped during spawned server startup.

### Backend Auth DB Assertions Failed Due Isolation Mismatch
Failed checks:
- Password hash lookup for `test@example.com` found no row in `brahm_test`.
- Expiring a refresh token in `brahm_test` did not affect the server, and `/api/auth/refresh` returned 200 instead of 401.
- Logout revocation lookup found no token row in `brahm_test`.
Classification: blocked by backend test isolation mismatch; not yet classified as product behavior failure.

### Frontend E2E Blocked: Playwright Chromium install failed
Expected: install browser dependencies and run Playwright suites.
Actual: Chromium download repeatedly reached about 40% and failed with `ENOSPC: no space left on device`.
Classification: local environment blocker. The authored F1 test cannot be executed until disk space is available or an existing browser executable is configured.

## Coverage Summary
Current coverage includes engine unit tests, a partial backend auth/security API suite, frontend production build verification, and authored-but-blocked F1 Playwright coverage. Broader backend route groups, frontend E2E, feature, mobile, and regression suites are pending or blocked by backend test isolation/server startup constraints and Playwright browser installation failure.

## Final Coverage Summary
- Engine unit tests: 72 passed / 3 failed.
- Backend API tests: 11 passed / 3 failed in the auth/security subset; remaining route groups not run.
- Frontend E2E tests: 0 passed / 1 blocked by missing Chromium and ENOSPC.
- Resilience tests: engine-level resilience covered with 2 failures for null/undefined normalization; full frontend/backend resilience phase not run.
- Feature tests: not run.
- Regression tests: not run.

## Critical Findings
- Engine malformed input resilience is not met for `normalize(null)` and `normalize(undefined)`.
- Generated frontend templates appear broken for project/entity URL interpolation.
- Backend server test isolation is unreliable: HTTP requests wrote to `brahm_db` while the test harness expected `brahm_test`.
- Browser-based frontend testing is blocked by local disk capacity.

## Untested Areas
Project CRUD, data isolation, dynamic data CRUD, route deduplication, CSV import, rate limiting, full frontend auth/dashboard/builder flows, i18n, GitHub export, mobile responsiveness, and hotfix regression checks remain untested in this pass because the backend could not be cleanly isolated and Playwright Chromium could not be installed.
